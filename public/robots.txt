# huddlr.co — crawl the marketing pages, keep the app/API out of the index.
# Marketing pages (/, /meeting-scheduler, /availability-poll, …) are static
# files under public/ and are crawlable by default. The Disallow rules below
# fence off the SPA's app/API routes, which the canonical also collapses to /.
User-agent: *
Disallow: /api/
Disallow: /dashboard
Disallow: /clients/
Disallow: /branding
Disallow: /*?poll=

Sitemap: https://huddlr.co/sitemap.xml
